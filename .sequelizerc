var path = require("path");

module.exports = {
  config: path.resolve("src", "shared", "infra", "database", "sequelize", "config", "config.json"),
  "migrations-path": path.resolve("src", "shared", "infra", "database", "sequelize", "migrations"),
  "models-path": path.resolve("src", "shared", "infra", "database", "sequelize", "models"),
  "seeders-path": path.resolve("src", "shared", "infra", "database", "sequelize", "seeders"),
};